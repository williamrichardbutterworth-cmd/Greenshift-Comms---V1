# The web app talks to the backend via the Vite dev proxy (/api -> :8080),
# so no env vars are needed for local development. Add VITE_* vars here only
# if you later point the build at a deployed API.
